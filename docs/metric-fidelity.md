# Metric Fidelity

- LUFS and LRA are approximated using a simplified RMS model.
- True peak is estimated via oversampling, not a full ITU-R BS.1770 chain.
- Use these values for regression detection rather than final loudness compliance.
