# Guarantees & Non-Goals

**Guarantees**
- Deterministic alignment and metrics for identical inputs.
- Produces HTML/JSON/Markdown reports.
- CI-friendly exit codes when thresholds fail.

**Non-Goals**
- Not a mastering-quality loudness analyzer.
- Does not replace human audio review.
