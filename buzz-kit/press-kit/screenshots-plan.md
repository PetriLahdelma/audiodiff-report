**Screenshots Plan**
- Terminal run of the quickstart command.
- HTML report overview with key metrics highlighted.
- JSON output snippet if using `--format json`.
- Optional: CI log showing a `--fail` threshold.
